import { NextResponse } from "next/server";import{ApiError,requireUser}from"@/lib/server/auth";import{loadApplicationData}from"@/lib/server/repository";export async function GET(){try{const user=await requireUser();return NextResponse.json({data:await loadApplicationData(user),user})}catch(e){const status=e instanceof ApiError?e.status:500;return NextResponse.json({error:e instanceof Error?e.message:"Unable to load data."},{status})}}
